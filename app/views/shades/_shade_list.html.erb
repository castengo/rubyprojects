<tr>
	<%= content_tag :td, "", style: "background-color: ##{shade.hex_color}; width: 100px;"%>
	<td><%= shade.name %></td>
	<td><%= shade.product.name %></td>
	<td><%= link_to "Add shade to look", add_shade_path(look, look_shade: shade, search: params[:search]), method: :put, remote: true %></td>
</tr>
